#include "dijkstra.h"
#include "graph.h"
#include "edge.h"
#include "BFS.h"

void BFSearch(Graph flightMap){
    vector<Edge> allEdges = flightMap.getEdges();
    for(int i = 0; i < allEdges.size(); i++){
        flightMap.setEdgeLabel()
    }
}
void BFSearch(Graph flightMap, Vertex v){

}
